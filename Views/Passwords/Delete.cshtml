@model PasswordManager.Models.PasswordEntry

@{
    ViewData["Title"] = "Delete Password";
}

<h1>Delete Password</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>PasswordEntry</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Title)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.EncryptedPassword)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.EncryptedPassword)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a href="@Url.Action("Index", "Passwords")" class="btn btn-secondary">Back to List</a>
    </form>
</div>
